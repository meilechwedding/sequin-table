Horizontal rule. Plain by default; pass `ornament` (or a `label`) for the brand's gold-hairline divider with a centered mark — use between editorial sections.

```jsx
<Divider />
<Divider ornament />
<Divider label="ST" />
```
